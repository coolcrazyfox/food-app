import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  categoryId: 0,
  openMenu: false,
  // // openSort: false,
  // sortType: {
  //   name: "Rating (min)",
  //   sortProperty: "-rating",
  // },
};
